# Secretos y configuración local — nunca subir con datos reales
.env
config.yaml
service_account.json
*.credentials.json

# Datos reales de clientes (sólo se versiona el ejemplo)
data/*.csv
!data/*.example.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
